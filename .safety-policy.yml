# Safety CLI policy. bankstatementparser-lsp has a small dependency tree
# (jsonschema, pygls, lsprotocol, bankstatementparser + its transitive deps); pin known false
# positives here with an expiry so they don't go stale.
security:
  ignore-vulnerabilities: {}
